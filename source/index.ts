// basic types
let id: number = 5;
let word: string = "Hello world";
let trueFalse: boolean = true;
let x: any = 'Hello';
x = "true" // Doesn't throw error

let numberArray: number[] = [1,2,3,4,5] //Array must only include numbers
let anyArray: any[] = [1,true, "3"] //Array must only include numbers

let person : [number, string, boolean] = [1, "2", false]